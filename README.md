# SLO vs Threshold Alerting Lab

Interactive educational PWA comparing threshold-based and SLO-based alerting.

## Scripts

- `npm run dev` starts a local server on port `4203`.
- `npm run build` builds static output into `dist`.
- `npm run preview` serves `dist` on port `4204`.
